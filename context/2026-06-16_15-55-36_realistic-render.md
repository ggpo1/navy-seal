# 2026-06-16 Реалистичный рендер морских котиков

## Что изменено

- Улучшен `SeaLionCanvas` для более реалистичной визуализации.
- Добавлены градиенты объема для тела и головы (`bodyGradient`, `headGradient`).
- Добавлена мягкая текстура шерсти через процедурные штрихи (`drawFurTexture`).
- Добавлены уши с внутренней частью (`drawEar`).
- Улучшен нос: ноздри и блик.
- Добавлены вспомогательные функции цветокоррекции (`lightenHex`, `darkenHex`).

## Результат

- Котики выглядят менее «плоскими» и более детализированными.
- Сохраняется совместимость со старыми метаданными.
- Фронтенд собирается успешно (`npm run build`).
